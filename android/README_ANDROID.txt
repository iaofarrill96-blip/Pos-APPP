Android native project should exist for a real build. Codemagic will run gradle assembleRelease.
If you need a full native android folder, generate it with 'npx react-native init' locally or use a full repo.
